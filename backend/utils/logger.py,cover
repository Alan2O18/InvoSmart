  # backend/utils/logger.py
> """
> Centralized Logging Configuration
  
> Features:
> - TimedRotatingFileHandler: Daily log rotation at midnight
> - Automatic cleanup: Keeps only last 7 days of logs
> - Dual output: Console (INFO+) and File (DEBUG+)
> - Consistent format across all modules
> """
> import logging
> import os
> from pathlib import Path
> from logging.handlers import TimedRotatingFileHandler
  
  # Default log directory (relative to project root)
> DEFAULT_LOG_DIR = Path(__file__).parent.parent.parent / "logs"
> DEFAULT_LOG_FILE = "backend.log"
  
  # Log format
> LOG_FORMAT = "%(asctime)s | %(name)s | %(levelname)s | %(message)s"
> DATE_FORMAT = "%Y-%m-%d %H:%M:%S"
  
  
> def setup_logging(
>     log_dir: Path = None,
>     log_file: str = DEFAULT_LOG_FILE,
>     console_level: int = logging.INFO,
>     file_level: int = logging.DEBUG,
>     backup_count: int = 7,
> ) -> None:
>     """
>     Initialize centralized logging configuration.
      
>     Args:
>         log_dir: Directory to store log files (default: PROJECT_ROOT/logs)
>         log_file: Name of the log file (default: backend.log)
>         console_level: Minimum level for console output (default: INFO)
>         file_level: Minimum level for file output (default: DEBUG)
>         backup_count: Number of days to keep old logs (default: 7)
>     """
>     if log_dir is None:
>         log_dir = DEFAULT_LOG_DIR
      
      # Ensure log directory exists
>     log_dir = Path(log_dir)
>     log_dir.mkdir(parents=True, exist_ok=True)
      
>     log_path = log_dir / log_file
      
      # Get root logger for 'backend' namespace
>     root_logger = logging.getLogger("backend")
      
      # Avoid duplicate handlers on repeated calls
>     if root_logger.handlers:
!         return
      
>     root_logger.setLevel(logging.DEBUG)  # Capture all, filter at handler level
      
      # Formatter
>     formatter = logging.Formatter(LOG_FORMAT, datefmt=DATE_FORMAT)
      
      # Console Handler - INFO and above
>     console_handler = logging.StreamHandler()
>     console_handler.setLevel(console_level)
>     console_handler.setFormatter(formatter)
      
      # File Handler - DEBUG and above, daily rotation, keep 7 days
>     file_handler = TimedRotatingFileHandler(
>         filename=log_path,
>         when="midnight",
>         interval=1,
>         backupCount=backup_count,
>         encoding="utf-8",
>     )
>     file_handler.setLevel(file_level)
>     file_handler.setFormatter(formatter)
>     file_handler.suffix = "%Y-%m-%d"  # Append date to rotated files
      
      # Add handlers to root logger
>     root_logger.addHandler(console_handler)
>     root_logger.addHandler(file_handler)
      
      # Log initialization
>     root_logger.info(f"Logging initialized. Log file: {log_path}")
  
  
> def get_logger(name: str) -> logging.Logger:
>     """
>     Get a logger instance for a module.
      
>     Usage:
>         from backend.utils.logger import get_logger
>         logger = get_logger(__name__)
      
>     Args:
>         name: Module name (usually __name__)
          
>     Returns:
>         Logger instance configured under 'backend' namespace
>     """
      # Ensure name is under 'backend' namespace for consistent handling
!     if not name.startswith("backend"):
!         name = f"backend.{name}"
!     return logging.getLogger(name)
