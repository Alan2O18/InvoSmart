  # backend/processing/pdf_engine.py
> """
> PDF 處理引擎 - 基於 PyMuPDF (fitz)
> 提供純函式的 PDF 操作，包含：
> 1. 頁面重組 (Page Reordering & Deletion)
> 2. 插入外部圖片印章 (Stamping)
> 3. 注入隱形文字層 (Searchable Text Injection)
> 4. 深度無損壓縮 (Deep Compression)
> """
> import logging
> import fitz  # PyMuPDF
> from typing import List, Dict, Tuple, Optional
> from pathlib import Path
  
> logger = logging.getLogger(__name__)
  
> class PDFEngineError(Exception):
>     """PDF 處理引擎的通用例外"""
>     pass
  
> def compress_pdf(input_path: str, output_path: str, quality: int = 90) -> bool:
>     """
>     對 PDF 進行深度無損壓縮與垃圾回收
>     使用 garbage=4, deflate=True
>     """
>     try:
>         doc = fitz.open(input_path)
          # 進行最高級別的垃圾回收 (4) 並啟用 deflate 壓縮
>         doc.save(
>             output_path,
>             garbage=4,
>             deflate=True,
>             clean=True
>         )
>         doc.close()
>         return True
!     except Exception as e:
!         logger.error(f"Failed to compress PDF {input_path}: {e}")
!         raise PDFEngineError(f"Compression failed: {e}")
  
> def reorder_pages(doc: fitz.Document, page_order: List[int]) -> None:
>     """
>     重排或刪除頁面
>     page_order: 包含期望的頁碼順序 (0-indexed)。例如 [2, 0, 1] 或是 [0] (若只要第一頁)
>     直接 modifying document in-place.
>     """
>     try:
>         doc.select(page_order)
!     except Exception as e:
!         logger.error(f"Failed to reorder pages to {page_order}: {e}")
!         raise PDFEngineError(f"Page reordering failed: {e}")
  
> def stamp_image(doc: fitz.Document, page_num: int, image_path: str, rect: Tuple[float, float, float, float]) -> None:
>     """
>     在指定頁面的指定矩形區域蓋上圖片印章
>     rect: (x0, y0, x1, y1) in PDF page coordinate points (72 DPI)
>     """
>     try:
>         page = doc[page_num]
>         fitz_rect = fitz.Rect(rect)
>         page.insert_image(fitz_rect, filename=image_path, keep_proportion=True)
!     except Exception as e:
!         logger.error(f"Failed to stamp image on page {page_num}: {e}")
!         raise PDFEngineError(f"Image stamping failed: {e}")
  
> def inject_text_layer(doc: fitz.Document, page_num: int, text: str, rect: Tuple[float, float, float, float]) -> None:
>     """
>     在指定頁面的矩形區域寫入隱藏的透明文字，以供搜尋
>     """
>     try:
>         page = doc[page_num]
          
          # 註冊內建的 CJK 字型，支援中日韓字集
>         fontname = "cjk"
>         try:
>             page.insert_font(fontname=fontname, fontbuffer=fitz.Font("cjk").buffer)
!         except Exception:
!             pass
          
          # render_mode=3 讓文字不可見但可以被選取和搜尋
          # insert_text 使用左下角的起始點, x0=rect[0], y0=rect[3] (但 PyMuPDF Y軸向下, 我們抓 bounding box 中間靠下)
>         x0, y0, x1, y1 = rect
>         start_point = fitz.Point(x0, y1 - 2)
          
>         page.insert_text(start_point, text, fontsize=11, fontname=fontname, render_mode=3)
!     except Exception as e:
!         logger.error(f"Failed to inject text layer on page {page_num}: {e}")
!         raise PDFEngineError(f"Text injection failed: {e}")
  
> def execute_commands(input_path: str, output_path: str, commands: Dict) -> bool:
>     """
>     根據前端傳來的 JSON 指令重建 PDF
>     commands = {
>         "page_order": [2, 0, 1],
>         "stamps": [
>             {"page": 0, "image_path": "...", "rect": [x0, y0, x1, y1]}
>         ],
>         "texts": [
>             {"page": 0, "text": "發票號碼", "rect": [x0, y0, x1, y1]}
>         ]
>     }
>     """
>     try:
>         doc = fitz.open(input_path)
  
          # 1. 執行排序/刪除
>         if "page_order" in commands and commands["page_order"]:
>             reorder_pages(doc, commands["page_order"])
  
          # 2. 執行蓋章
>         if "stamps" in commands:
>             for stamp in commands["stamps"]:
>                 stamp_image(doc, stamp["page"], stamp["image_path"], stamp["rect"])
  
          # 3. 執行隱形文字寫入
>         if "texts" in commands:
>             for text_data in commands["texts"]:
>                 inject_text_layer(doc, text_data["page"], text_data["text"], text_data["rect"])
  
          # 4. 深度壓縮儲存
>         doc.save(
>             output_path,
>             garbage=4,
>             deflate=True,
>             clean=True
>         )
>         doc.close()
>         return True
!     except Exception as e:
!         logger.error(f"Failed to execute commands on {input_path}: {e}")
!         raise PDFEngineError(f"Command execution failed: {e}")
