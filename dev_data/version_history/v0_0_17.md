# 版本紀錄 - v0.0.17 (PDF 拔除 + Resplit RAW 對齊 + 印章全人工)

日期：2026-04-13

---

## 🎯 重點更新 (Key Focus)
本次先完成 v0.0.17 的高風險與高耦合區塊：
- 徹底拔除舊版獨立 PDF 蓋章排版功能（後端 + 前端 + 測試）
- Resplit 改為「RAW 大圖優先」座標系
- 印章建檔流程改為「全人工拉框」

尚未進入：`FileOps -> FileService/ImageService/CacheService` 的完整拆分與刪檔。

## ✨ 主要更動 (Implemented)

### 1) PDF 模組拔除
- 後端刪除：
  - `backend/routers/pdf.py`
  - `backend/engine/pdf_worker.py`
  - `backend/processing/pdf_engine.py`
- 後端更新：
  - `backend/main.py`：移除 PDF router import / include
  - `backend/engine/core.py`：移除 PDF queue/worker 與 enqueue 相關方法
  - `backend/engine/file_ops.py`：移除 `add_pdf_files`
- 前端刪除：
  - `frontend/src/views/PdfEditorView.vue`
  - `frontend/src/components/PdfWorkbench.vue`
  - `frontend/src/views/KanbanView.vue`
- 前端更新：
  - `frontend/src/router/index.js`：移除 `pdf-editor` 與 `kanban` route
  - `frontend/src/views/HomeView.vue`：移除 PDF 看板入口
  - `frontend/src/views/ProjectDetailView.vue`：移除 PDF 上傳與 PDF 區塊
  - `frontend/src/services/api.js`：移除 `uploadPdf/executePdfCommands/downloadPdf`

### 2) Resplit RAW 對齊
- 前端：
  - `frontend/src/components/ResplitModal.vue`
  - 改為使用 RAW 來源預覽，透過 split 檔名推導 raw stem
- 後端：
  - `backend/engine/file_ops.py`
  - 新增 RAW 來源解析邏輯：`_resolve_resplit_raw_source`
  - `detect_job_sub_rects/apply_job_resplit` 改為 RAW 優先，找不到才 fallback split
  - `backend/routers/files.py`：`/preview/raw/{filename}` 支援以 stem 反查 raw 檔

### 3) 印章全人工化
- 前端：
  - `frontend/src/components/StampAssignDialog.vue`：從三步改兩步，移除自動偵測流程
  - `frontend/src/store/stamp.js`：移除 `detecting` 與 `detectStamps`
  - `frontend/src/services/api.js`：移除 `detectStamps()`
- 後端：
  - `backend/routers/stamps.py`：刪除 `POST /stamps/detect`
  - `backend/engine/stamp_service.py`：移除 preview base64 helper
  - `backend/processing/stamp_processor.py`：移除 `detect_stamps` 與 edge/IoU 輔助邏輯，保留手動裁切與去背核心

### 4) 測試同步更新
- 刪除：
  - `tests/test_routers_pdf.py`
  - `tests/test_pdf_engine.py`
- 調整：
  - `tests/test_engine.py`（worker 啟動數量由 2 改 1）
  - `tests/test_engine_worker_loops.py`（移除 PDF worker 測試）
  - `tests/test_routers_stamps.py`（移除 detect endpoint 測試）
  - `tests/test_stamp_processor.py`（改測手動 extract 流程）
- 新增回歸：
  - `tests/test_engine_file_ops.py`
  - 新增 RAW 優先來源測試：
    - `test_detect_job_sub_rects_prefers_raw_source_by_split_stem`
    - `test_apply_job_resplit_prefers_raw_source_by_split_stem`

## ✅ 驗證結果 (Verification)

### 後端測試
- 目標回歸（engine worker/main/stamp/resplit/files）：全部通過
- 全量（排除既有遺失腳本測試）：
  - `pytest --ignore=tests/test_migrate_flatten_jobs_script.py --ignore=tests/test_migrate_images_script.py`
  - 結果：`477 passed`
- 全量直接執行現況：
  - `pytest`
  - 結果：2 個既有 collection errors（遺失 scripts 模組），與本次改動無關

### 前端驗證
- `npm run build`：通過

### 架構守門
- `python scripts/check_architecture_guards.py`：通過

## ⚠️ 未完成項目 (Pending)
- `FileOps` 完整拆分（`FileService` / `ImageService` / `CacheService`）尚未開始。
- 目前維持功能正確優先，先完成 PDF/Resplit/Stamp 三條線的行為收斂與回歸。

---

下一步建議：進入 v0.0.17 Phase 2，分批將 `file_ops.py` 遷移至新 service 檔並逐批替換 router 呼叫。