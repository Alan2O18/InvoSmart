# AI Agent Lab - VLM-First Dependencies
# Python 3.12+ Compatible

# --- Core Web Framework ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
python-dotenv>=1.0.1
aiofiles>=23.2.1
requests>=2.31.0
httpx>=0.27.0
pandas>=2.2.0
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# --- AI & VLM Client ---
openai>=1.14.0
# used for VLM calls (Gemini, OpenRouter, etc.)

# --- Image Processing ---
numpy>=1.26.0
opencv-python-headless>=4.9.0
PyMuPDF>=1.24.0
# headless version is smaller and ideal for server/cloud envs

# --- QR Code Decoding ---
# QReader provides robust detection using YOLOv8-nano
qreader>=3.12
rapidocr-onnxruntime>=1.3.0
# Note: This will install 'ultralytics' which includes a CPU-only version of torch.
# If you want to avoid torch completely and accept lower QR accuracy, 
# you can remove qreader and only use pyzbar (requires code changes).

# --- Data Validation ---
pydantic>=2.6.0
pydantic-settings>=2.2.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.1

# --- Database & Migration ---
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
alembic>=1.13.0
