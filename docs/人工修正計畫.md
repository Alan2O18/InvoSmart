# 人工修正功能計畫

## 設計決定

| 項目 | 決定 |
|------|------|
| 佈局 | 水平分割 + 可拖動調整大小 + 標籤頁最大化 |
| 編輯器 | 簡單 textarea |
| LLM重生 | 從人工修正文字調用 `extract_data` |
| 區域OCR | 延到 v2 |
| 歷史 | 前端 Ctrl+Z |

---

## UI 佈局

```
┌──────────────────┬──────────────────┬──────────────────┐
│ [發票圖片]        │ [LLM結果]        │ [人工修正]        │
│                  │                  │                  │
│  縮放/旋轉        │  JSON顯示        │  Textarea        │
│  ←拖動調整大小→   │ ←拖動調整大小→   │  保存/重新生成     │
└──────────────────┴──────────────────┴──────────────────┘
標籤列: [圖片] [LLM] [修正] - 點擊可最大化面板
```

---

## 後端修改

### 資料庫: jobs 表新增欄位
```sql
manual_ocr_text TEXT,
manual_updated_at REAL
```

### 新增 API 端點

| 方法 | 路徑 | 說明 |
|------|------|------|
| GET | `/{id}/jobs/{job_id}/details` | 獲取完整任務資料 |
| PUT | `/{id}/jobs/{job_id}/manual` | 保存人工修正文字 |
| POST | `/{id}/jobs/{job_id}/regenerate_from_manual` | 從人工文字重跑LLM |

---

## 前端修改

### 新增: `JobEditorView.vue`
- 路由: `/project/:id/job/:jobId/edit`
- 三個可調整大小的面板 (CSS flexbox + 拖動把手)
- 標籤列可最大化/最小化面板
- Ctrl+Z 撤銷

### API: 新增到 `api.js`
```javascript
getJobDetails(projectId, jobId)
saveManualText(projectId, jobId, text)
regenerateFromManual(projectId, jobId)
```

---

## 實現步驟

### Phase 1: 後端
- [ ] 新增 `manual_ocr_text`, `manual_updated_at` 欄位
- [ ] 新增 `get_job_details()` 到 TaskManager
- [ ] 新增 `save_manual_text()` 到 TaskManager
- [ ] 新增 API 端點

### Phase 2: 前端
- [ ] 建立 `JobEditorView.vue` 三面板頁面
- [ ] 新增面板之間的拖動調整大小把手
- [ ] 新增標籤列切換面板
- [ ] 新增 Ctrl+Z 撤銷
- [ ] 新增 API 調用
- [ ] 在 Jobs 表格新增 "編輯" 按鈕
